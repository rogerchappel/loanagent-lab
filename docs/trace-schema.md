# Decision trace schema

Each trace contains `schemaVersion`, `applicationId`, `decision`, `score`, `metrics`, `factors`, `hardFlags`, `caveats`, and `reviewerChecklist`.

The schema is designed for deterministic tests and reviewer handoffs, not automated real-world credit decisions.
