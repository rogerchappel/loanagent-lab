# Examples

```sh
loanagent-lab inspect fixtures/sample-good --format markdown
loanagent-lab inspect fixtures/sample-review --output out/review
loanagent-lab fixtures
```

Expected recommendations: `sample-good` approves, `sample-review` needs human review, and `sample-thin` declines in the synthetic policy.
